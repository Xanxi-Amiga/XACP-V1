# Third-party notices

`StartUp.c` and `LibInit.c` retain their original comments stating that they are based on **CLib37x by Andreas R. Kleinert**. The attribution is preserved unchanged.

The source also uses standard AmigaOS SDK interfaces supplied by the target development environment; those SDK headers are not included here and remain subject to their own terms.

MPEG decoding itself is not included in this engine source tree. It is performed by the XACP firmware service on the ZZ9000 ARM; the firmware and its decoder components are documented and licensed separately elsewhere in the XACP repository.
